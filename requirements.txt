# Core pipeline runtime (local, free — runs the whole lakehouse)
pandas>=2.1
numpy>=1.26
pyarrow>=14.0
duckdb>=1.1
dbt-core>=1.10.5
dbt-duckdb>=1.10
pyspark==3.5.3

# Serving
streamlit>=1.50
plotly>=5.22

# Dev / CI
pytest>=8.0
ruff>=0.6

# Orchestration is provided via Docker (see docker-compose.yml). To run Airflow
# locally instead, install it in its own environment:
#   pip install "apache-airflow==2.9.3"

version: 2

sources:
  - name: raw
    description: "Conformed clean-layer tables loaded into the warehouse `raw` schema."
    schema: raw
    tables:
      - name: products
      - name: orders
      - name: order_items
      - name: inventory_snapshots
